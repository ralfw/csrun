#functions
int Add(int a, int b) {
    return a + b;
}

#test addition mit positiven zahlen
Assert.AreEqual(3, Add(1,2));

#test addition mit negativer zahl
Assert.AreEqual(3, Add(4,-1));

#test fehlschlagender test
Assert.AreEqual(1, 2);